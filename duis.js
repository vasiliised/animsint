class ItemHandler {
    grabItem(e) {
        console.log('Item grabbed!', e);
        // Function body: Perform actions to handle the grabbed item
    }
}

// Usage:
const handler = new ItemHandler();
handler.grabItem(event);
